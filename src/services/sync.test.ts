import{describe,expect,it}from'vitest';import{countdownRemaining,nextRoomState}from'./sync';describe('synchronization',()=>{it('uses a shared target timestamp',()=>expect(countdownRemaining(6000,3100)).toBe(3));it('transitions room states',()=>{expect(nextRoomState('idle','create')).toBe('waiting');expect(nextRoomState('connecting','connected')).toBe('connected');expect(nextRoomState('connected','left')).toBe('disconnected')})});
