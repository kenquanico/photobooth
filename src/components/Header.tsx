import { Code2, Menu, X } from 'lucide-react';import { useState } from 'react';import type { Page } from '../types';
export function Header({go,page}:{go:(p:Page)=>void;page:Page}){const [open,setOpen]=useState(false);return <><div className="announcement">Your photos stay on your device — always.</div><header><button className="brand" onClick={()=>go('home')}>petal<span>booth</span></button><button className="menu" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button><nav className={open?'open':''}>{[['home','home'],['layouts','layouts'],['pair','pair devices']] .map(([p,l])=><button className={page===p?'active':''} onClick={()=>{go(p as Page);setOpen(false)}} key={p}>{l}</button>)}<a href="https://github.com" aria-label="GitHub" target="_blank"><Code2 size={18}/></a></nav></header></>}
